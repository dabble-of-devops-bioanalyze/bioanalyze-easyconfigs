name: easybuild
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - pip
  - click
  - pandas
  - pyarrow
  - dask
  - pip:
      - devtools
      - colorlog
      - quilt3[catalog,pyarrow]
