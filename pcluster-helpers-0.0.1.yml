name: easybuild
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - pip
  - click
  - pydantic
  - rich-click
  - pandas
  - pip:
      - nf-core
      - devtools
      - colorlog
      - aws-pcluster-helpers
      - aws-pcluster-slurm-spawner
