name: easybuild
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - pip
  - click
  - pydantic
  - rich-click
  - pandas
  - pip:
      - nf-core
      - devtools
      - colorlog
      - aws-pcluster-helpers>=1.0.8
      - aws-pcluster-slurm-spawner>=1.0.8
