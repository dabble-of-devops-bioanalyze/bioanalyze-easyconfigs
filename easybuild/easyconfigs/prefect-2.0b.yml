name: prefect2
channels:
  - conda-forge
dependencies:
  - python=3.9
  - prefect>=2
  - pip
  - pip:
      - aws-pcluster-helpers
      - aws-pcluster-slurm-spawner
      - aws-pcluster-dask-gateway
      - aws-pcluster-helpers
      - aws-pcluster-slurm-spawner
      - aws-pcluster-dask-gateway
      - s3fs
      - prefect>=2
      - prefect-aws
      - prefect-shell
      - prefect-dask
